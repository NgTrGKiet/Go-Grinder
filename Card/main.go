package main

func main() {
	// var card string = "Ace of Spades"
	// cards := deck{"Ace of Diamonds", newCard()}
	// cards = append(cards, "Three of Diamonds")

	// fmt.Println(cards)
	// cards := newDeck()
	// cards.print()
	// deal(cards, 5)
	// hand, remainingDeck := deal(cards, 5)
	// hand.print()
	// remainingDeck.print()

	// greeting := "Hi there"
	// fmt.Println([]byte(greeting))

	// cards := newDeck()
	// // fmt.Println(cards.toString())
	// cards.saveToFile("queencard")
	// // cards := newDeckFromFile("queencard")
	// // cards.shuffle()
	// cards.print()
}
